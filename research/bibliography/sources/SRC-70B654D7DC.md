> Source: https://www.youtube.com/watch?v=HfkcXxC-JUs

uh thank you so much for this kind of introduction thank you all uh for coming it's always great to be here uh at sford to learn about all the exciting research uh that is uh Happening Here I actually didn't come all the way from surc I'm spending some time in Cambridge right now uh in Boston and the East Coast um i' love to share some work that we've been doing uh in a group around uh safe and efficient learning uh in the real world and of course this is Joint work with a fabulous uh group of PhD students postdocs and Senior collab just who I'll acknowledge as we go along you probably all agree that there is these are really exciting times for machine learning and artificial intelligence sort of far beyond classifying images translating languages these models now really exhibit a pretty impressive level of generality you can ask them to compose poems about uh and rap songs around sub module optimization right there's examples of creating photo realistic uh videos from textual descriptions and the like so this is certainly super exciting what does it take to sort of bring these advances uh to the physical world right applications like human robot interaction uh advancing recommendations in healthcare scientific discovery Precision farming and the likes right so as soon as you sort of talk about the real words Stakes are high right failures uh are much more problematic and so what we've been really interested in is how can one enable learning agents to actually learn by interacting uh in the real world and I want to share some of the thoughts that we've been exploring in this space now the central Paradigm in machine learning uh in these contexts is reinforcement learning which of course this is abstract model of an agent that's situated in an environment which can affect by carrying out actions that cause the environment to change its state in a movian fashion and the agent to derive some reward right and of course the same model has been studied in controls so adaptive control um um approxim dynamic programming and the likes and a key challenge in this domain is that the agent doesn't know how the world works so it faces The Dilemma of trading expiration so experimenting with actions to learn about their consequences and exploitation using what it learned in order uh to do well okay and so we've also uh seen these pretty remarkable advances a few years back in uh gameplay but also more recently in challenging robotics applications like our colleagues in Sur for example use them to have robots go hike right or fly drones faster than human pilots and so on so it's certainly pretty exciting advances in the space now RL is sort of portrayed in this cartoon that the agent goes out and learns by interacting with the real world but of course in Truth uh the agent uh lives in a matrix right so the agent really needs an accurate simulator that describes how the world uh Works in a lot of applications that um like those that I've mentioned before there's the best approximates simulators of how the real world would work right and applications like scientific discovery you do quite a bit of work sort of around self-driving labs and autonomous experimentation and so I mean you that's the whole purpose of science to learn kind of how the world works and so the question is now if you go to these domains then um exploration right is costly you need to carry out experiments so you really want to be sample efficient and uh potentially dangerous right and so we've been really interested in how can learning agents learn by interacting with the real world effici ly and safely and that's what I'm want to talk a little bit about in this presentation so I start with a line of work that we've been pursuing now for a couple years around safe basion optimization and then build on that uh to talk about sort of some recent directions that we exploring and so let me start with the motivating example so this is a collaboration uh with the poer Institute a large uh research facility uh in Switzerland uh this is the swissfel free electron laser it's a 700 met long linear accelerator that generates um x-ray pulses of extremely short duration so fto duration one millionth of a billionth of a second so you can use this to image very fast processes like molecules turning each other in context of a chemical reaction super important applications uh from drug design to materials Discovery to many others it's also a pretty complicated device so the beam properties are dependent on a carefully arranged set of magnets that partly actually are physically moved around the beam properties uh vary uh quite substantially based on subtle environmental conditions like temperature humidity have to be adapted based on uh Target application requirements and so on so it's not just that you set this up once and you're done but you have to constantly sort of adapt it uh it's also safety critical so if you're not careful what you do uh you can basically damage the machine right damage the magnets uh and destroy it so really have to be careful but fortunately of course there's uh monitors all over this device right that sort of monitor how close you get uh in terms of violating some of the constraints another question is kind of so maybe the other thing I want to mention is in principle we can simulate the underlying physics but the simulations are extremely expensive at this level of accuracy and given the fact that you really need to adapt to different enviral conditions and so on you can't really run and prepare this in in simulation okay so essentially you have to do this tuning and experimentation on the device while it's running and so one way to sort of think about this very abstractly as sort of trying to optimize a black box right this electron laser by injecting some uh tuning parameters th over time uh and obtaining some measurements about the rewards right namely uh beam intensity say so other targets you care about as well as sort of how close you are to violating some of the constraints right through these beam loss monitors right given us information you'd like to close the loop the problem is you never you don't know the report nor the constraints ahead of time right you really need to take your observations into account in order to drive experimentation so in a way what you want to do is you want to solve sort of this nonlinear constraint optimization problem optimizing this unknown reward subject to unknown constraints while maintaining feasibility every step along the way and of course in general this is a completely opposed problem right without any sort of uh prior assumptions uh this is hopeless so one way to impose prior assumptions is to kind of take a ban perspective on this and try to model uncertainty in the unknown objectives and their unknown constraints and that's exactly sort of the philosophy behind Vision optimization right where one endows the unknown reward and constraint with a stochastic process prior which one can then use in order to guide experimentation while being cautious and so kind of the first uh result we had about this a while back is basically operationalizing this idea so we model the reward and constraints in a way as samples of the pro stick model so kind of a classical default and based optimization uh is Goan process models uh that we analyze quite a bit uh you can also use B Network models and we'll talk a little bit more about kind of new network based approaches later uh and then you can use the uncertainty to on one side reasonable constraint satisfaction so for example if you use a pessimistic estimate of your constra right the lower confidence bound about the constraints right you can sort of statistically um inner approximate the feasible region right uh that holds with high probability as long as these models are well calibrated and similarly you can use kind of your confidence estimates about the rewards in order to for example reason about what are subsets of the parameter space that are plausibly optimal right where kind of the optimistic estimate exceeds the best uh as of pessimistic lower bound right and so a natural kind of algorithm would be to basically try to explore within kind of this plausibly optimal region but being constrained in this red sets right this feasible region the problem is that then you quickly get stuck in subal Solutions so what you also have to be care about is in a way to sort of try to gather information about what is feasible in the first place and that's captured using this notion of expanders of course everything mathematically defined in the paper um where you sort of have an incentive to actually try to explore uh the boundary right and discover new regions in the space uh that you can statistic Al certifi to be safe okay and eventually you might find better Solutions this way that's sort of the cartoon picture right of course the details are in the paper um but in order to kind of make ideas like this work you really need these models to be well calibrated so you need to make sure that this true unknown objective and true uh unknown constraint are actually contained within the credible sets of the basion model there also similarly kind of frequentist perspectives uh on this and so now of course if you kind of were a ban you had a well perfectly well calibrated model could just take a point take your mean add some multiple times your standard deviation and enumerate kind of your percentiles on that marginal distribution but of course your model might be miscalibrated right the true objective might light outside of these credible sets and moreover you want to ensure that these bounds hold actually everywhere in the domain and uniformly over time and that's so constructing models that produce these kind of well calibrated confidence sets that hold reliably uniformly over time sort of I in time valid that's a very interesting important sort of thetical question and we've been spending quite a bit of time sort of looking at these types of problems and turns out it's actually possible to calibrate these uncertainty sets uh so you need to in a way take into account kind of the how in a way the mismatch between the underlying objective and these confidence sets as well as some notion of capacity right that uh in a way uh you can use in order to get uh uniform bounds over the uh domain so there's quite a bit of work on the statistical side that I'm not going to dwell on too much in this talk on sort of trying to really understand in what sort of circumstan is you can analyze this so the first Works where around ction process models and can extend this to some extent to neural network based models and other models as well and also other types of likelihoods and now under these types of regularity uh conditions regularity assumptions uh one can actually Analyze This algorithm that I've described to you before and establish bounds on how many samples you need in order to find a near optimal reachable solution while ensuring constraint satisfaction with high probability every step along with it okay and so um variance of this algorithm they actually got to deploy on this real device so here's a picture of uh my students uh Johannes and moir together with our collabor uh Nicole is sitting in the operating room of this machine running uh the code in the background um and they would do experiments that they would start with parameter settings that were chosen by the domain experts kind of detune them uh so that the whole system would perform badly and then try to recover right using different strategies right and so there's kind of local search based strategies that are that are used used in this domain and you can actually do quite a bit better by the sort of more Global optimization that safe basion optimization affords uh at the same time actually have these uh uh these uh safety guarantees and this is kind of an early work there's been now quite a bit of applications of kind of the safe basan optimization Machinery so we've did another collaboration uh kind of around kind of large scale scientific instruments this is high intensity proton accelerator operated by that same uh Research Center that's even more safety critical in some sense so you really have to be careful about what happens and here the system really allows you to actually find much better Solutions without kind of generating beam interruptions um we've been using this to uh tune uh gain parameters for high Precision uh motors used for industrial man uh manufacturing tasks uh tune control gate parameters for quadruped robots and the likes and so now what I want to do in sort of the remainder of this talk is to kind of tell you about some ideas towards scaling these ideas to sort of richer and more more complex applications and so one uh first topic that I want to talk a little bit about is sort of where kind of does the prior come from right so inv basion optimization really the key requirements is you need a problemistic model right that sort of uh encodes your prior assumptions uh about how the unknown objective rewards looks like and if there's one thing right to learn from all the advances in deep learning and now also in generative AI uh it's really that if you have enough data kind of learning good representations you can often do better than handd designed uh representations right and so there in addition right once you learn good representations from one task often you can adapt it or fine unit uh to related tasks right like for example take uh image classification models trained on natural images and just using a little bit of say medical data you can sort of adapt uh the the model uh in a very simple efficient manner so the question is can you do something like this for sequential decision- making tasks like uh Vision optimization okay and so there's a very natural perspective uh on this right through the lens of beian meta learning there's Al some really interesting work that's happening here at Stanford sort of in this in this domain uh where uh you instead of starting with a handd designed prior distribution right maybe a Goan process prior with a fixed kernel that you choose uh to step up one level in the basian hierarchy to take a collection of tasks right from related domains and then try to starting with the hyper prior on uh the parameters of your prior distribution apply ban inference or approximations to obtain a hyper posterior that in a way absorbs all the information from related tasks and that you can hopefully adapt in a very simple efficient manner to new tasks right so that's a classical idea the question is sort of how to get this to work with modern models and so the canonical approach of how to adress this um is to basically start with the hyper prior on the parameters of your prior distribution okay and so you can do this for example for shared linear regression right maybe you have different linear models there sort of a common set of coefficients across all tasks and then kind of you learn how to deviate sort of across different tasks that you want to fit the question is how to do this in sort of this basically nonparametric setting involving neuron Nets and now the F Choice that's often done in the literature is to start with a very uninformed kind of isotropic Prior just put a g maybe on the coefficients the weights of the newal network the problem with this is It's kind of very hard to reason about what kind of Prior assumption this really imposes on this function space and you also get often very poor kind of empirical side effects if you do something like this and so what we've been exploring certain of alternative is to in way and code prior assumptions directly on the space of functions and the motivation in a way is the gion processes behave in kind of a very interpretable fashion so what typically happens is close to your data you're confident and as you go away from your data your uncertainty goes up the question is how can I express these types of Prior assumptions in this context of ban meta learning with NEC models and so that suggests kind of a very natural perspective towards meta learning basically trying to kind of learn a model of this underlying stochastic process directly that governs the space of reward function con and constraints now that seems like an even harder problem right you start with a problem of specifying a prior over a large but finite set of parameters now you want to learn a prior distribution of entire functions but fortunately stochastic processes are fully defined on their marginals so you really only need to characterize them where you use them in some sense and moreover you don't actually need to learn the full underlying prior distribution of our functions so turns out s of all the standard approximate inference techniques for basion deep learning all they really need is What's called the score so the gradient of the log dens of this stochastic process prior on a set of points where you evaluate them so as long as you can sort of get your hands on that score you're in business and that's the perspective that we've been exploring so essentially what you want to do is to take a bag of tasks right um maybe observations in past tuning sessions of this three electron laser to stay with this running example and then sort of try to learn something about this underlying stochastic process prior okay and so what he said is we really need to control the marginals of this underlying stochastic process right so the one dimensional marginals of course are just the function responses say at a particular point x right this is one example and of course we can look at higher dimensional marginals like a two- dimensional example of X Point basically the function response at points X1 and X2 and how they're jointly distributed for gausian processes all these marginals are gausian right joint gausian that's a characterizing property but in general of course they need not be and so the question is how can we sort of learn a flexible model that in a way allows us to predict enough about sort of the score um where we want to use these models to make predictions okay and so to this end we use a very flexible neur architecture basically a Transformer based model uh that sort of directly predict seeks to predict the score of where we use it uh for the stochastic process prior and in order to predict the score we basically use score matching right so of the key Machinery used behind uh diffusion models okay and so uh that actually works really quite well so in the iar paper last year we do a fairly extensive empirical study gooss a whole range of different uh data sets app relations a whole range of different meta learning and not meta learning based approaches some of them really designed for basan meta learning and a certain quantification some for constructing Point estimates and really stress test them in kind of this regime that you have very few examples per task and possibly also only very few tasks right sort of this really challenging uh challenging domain where you really need to extract as many bits as you can from related observations and there so there's one data set is about this uh three electron laser example that I mentioned right so here tasks correspond to different tuning sessions um uh there's a medical time series uh data set you look at so that each task is a patient uh monitored in the Intensive Care Unit you want to learn a joint model that in a way explains all the data but personalize it with respect to each patient uh there's a whole bunch of other data sets we look at in this context as well and this model performs uh really uh quite well uh in terms of the flexibility both in terms of getting high accuracy and also getting uh low calibration error now um of course there lots of interesting theoretical questions of what one can do about meta learning uh probabalistic models in particular for sequential decision making where you don't get IED data in general it's a very hard problem I just want to give a quick shout out to my student pan who's been looking at more theoretical questions and more parametric um regimes of this uh of this question um so analyzing this algorithm end to end I think is is is quite challenging now uh one thing I want to talk about is sort of how does this interface with tasks where safety is important whether you really not just want to get good prediction but you really want to make sure that you produce conservative and certainty estimates and so there um what we've been looking at is basically encoding a little bit more structure in terms of this hyper prior and so one way to do so is to use the gaan process as sort of a hyper prior and then basically shaping it through sort of key hyper parameters so one key parameter is basically kind of a scale parameter the variance of the function responses another key parameter of course is the length scale right and if you sort of have large length scales single observations in a way reduce the uncertainty a lot right so associated with getting a lot of information uh but of course uh might uh be unsafe right if you in a way sort of predict uh too large of reduction of uncertainty in contrast small length scales are much more uh conservative uh but uh of course uh might be uh might lead to a more R robust behavior and so you can sort of look at this kind of basically um face diagram here in some sense right where you compare these hyper parameters along length scales and the variance right and in a way uh if you um decrease length scale and increase the variance parameter right the model becomes more conservative U but of course uh correspondingly slower if you increase the length scale but decrease the variance uh you get unsafe Behavior okay and so the question is of course how do you find The Sweet Spot and so ideally what you'd like to do is you'd like to find such parameter settings such that in a way you ensure constraint satisfaction sort of during learning while at the same time minimizing regrets or maximizing performance at the corresponding tasks that you would want to run and of course that you can't really do uh with in the absence of actually collecting data and deploying the algorithm and so what you show in the scroll paper is that it's possible to sort of find computationally efficient proxies for these metrics that you actually care about okay in particular what you can look at is on rated tasks what's the calibration U of the model so in a way the coverage of the corresponding confidence confidence sets that you get out of this model at the same time you can also look at in a way the efficiency of the prior so the amount of reduction in variance okay and so these are very correlated across these different tasks and you can use this idea in order to basically do some sort of Frontier search algorithm to in a way solve this constraint optimization problem that you in a way um maximize the informativeness of the prior while ensuring calibration on past data sets okay and so that's in a way is basically very compact parameterization of this hyper prior that sort of shapes your stochastic process predictions uh that you can then navigate efficiently experimentally we've found fairly extensive uh study including experiments on on Hardware uh experiments uh where uh basically using these meta learning ideas not only you get sort of conservative predictions which is what you would want but you get substantial acceleration in terms of performance so if you don't use meta learning uh in a way you take much much longer in order to find good parameter settings then using this uh this correspondingly optimized prior okay so this is some of the work that we wanted to talk about about um meta learning so reusing information from related tasks and now the question is um how can we make use of it in tasks Beyond Bean optimization and I want to talk a little bit about is um how to lift ideas from basian optimization to learning based control in particular sort of ban model based uh reinforcement learning and so B optimization is really sort of a blackbox method right you way have this black box you put in a parameter you measure something as an output right but in so dynamical systems you really would want to make use of OB observations how the system actually works right State observations along the way okay and so one way to sort of think about this is to basically model uh an unknown dynamical system right governed by basically a mark of transition kernel in the illustrations here we'll actually talk about deterministic systems but you can scale these ideas to stochastic systems as well and uh what you're going to do is you're basically quantifying uncertainty in the Dynamics of this uh dynamical system okay so these confidence sets that we talked about before so they basically govern the transition kernel right the our epistemic uncertainty about the next state St plus one that you would get to if carry if you carry out action at in state St okay now you can sort of think about this way right suppose you want to start from some starting State maybe go to some gold state while avoiding unsafe States uh what you can do is you can basically propagate sort of the epistemic uncertainty in this unknown Dynamics model um but since you're uncertain about where you end up right uh you can't right you don't really know where you actually will up when when you carry out this policy but of course once you deploy it you collect information your uncertainty is going to contract and you can sort of replan and hopefully be less conservative right and obtain improved Behavior okay and so what we can basically do is use these confidence sets both for reasoning about expiration right so you might want to think about planning an action sequence that will plausibly allow you to achieve the task while always going to avoid visiting unsafe States right with respect to the confidence uh estimates afforded by this problemistic model okay the only question is how to make this use make this work with algorithms for reinforcement learning but and scale them to uh to to complex tasks and complex control policies so first I'll just talk about kind of optimistic exploration so how should we go about in a way using reinforcement learning for introspective planning but being very careful about interactions with the Real Environment so kind of learn good behavior very quickly okay and so the protocol we're going to look at is basically this uh modelbased uh RL uh Paradigm in the episodic setting where every round we carry out an episode we have a current policy that we interact with the Real Environment so we deploy it we're going to collect data and we use this data in order to refine our estimates of uh the transition colel of the Dynamics model and then we use the epistemic uncertainty around this transition model in order to replan our policy and for latter that is a purely introspective task right so you basically use the estimates including the epistemic estimates of the uncertainty uh in order to uh basically plan a new policy and this is where we want to make use of kind of state-of-the-art techniques in model three reinforcement learning Pol policy gradient techniques uh and so on right so we want to kind of estimate a simulator where we can spend computation whatever you want in order to find a good policy but then be very careful when deploying it on the real system okay so how would we do this so one way of how one can think about doing this in mod model based RL is to basically try to optimize our objective and we discounted uh returns understand that of approximate dynamic programming formulation but the problem is we don't know we the transition kernel so typically right you would assume you can actually at least sample from the transition kernel you don't know what the transition model is so what one can do is one can sort of consider an optimistic transition kernel right there's a set of plausible transition models dyamics models and try to optimize the policy that under its most plausible realization you do well okay so now the problem is solving this optimization problem is really difficult in general if you have complex things like C process based Dynamics models uh neural network based Dynamics models and so on you have a very complex non-convex uh constraint set that you have to uh have to optimize over and so as a consequence sort of state-of-the-art techniques that use epistemic uncertainty about the unknown transition kernel typically just average out over it right optimize of an expectation for example so this at the heart of from the pilco method as classical method for model based RL with gion processes the pets method using neural network ensembles uh and so on that's basically just average over year uh models basically sort of certainty equivalent control in some sense okay and so what we figured out is a way of um actually carrying out optimistic expiration so propagating the uncertainty in the Dynamics model but reducing this problem to just a standard approximate dynamic programming problem to which one can apply standard model free um say policy gradients algorithms for introspective planning uh and let me just explain sort of how that uh roughly works so here's a simple kind of onedimensional cartoon so uh suppose we start in our starting State uh maybe a zero and we want to uh reach a spar reward state so the y-axis is kind of the one dimensional State space in this cartoon the x-axis is time okay and so now for illustration again let's just look at deterministic Dynamics so the only uncertainty is of epistemic nature that we have lack of information about the consequences of our actions so it means that if we consider a candidate policy Pi that is going to take us to some kind of nominal next States predicted next States then we'll have some epistemic uncertainty of where we would really end up okay and so in this formulation I talked about before now you so have to do this for all plausible dynamics that you will would unroll for it so instead what we're going to do is we'll let the agent control its luck okay so we'll let the agent choose where within this one-step confidence sets it wants to end up with okay so you can choose where within sort of these plausible next States it will be now this gives us an sort of optimistic next state and basically using these uncertainty sets where you essentially describe kind of rectangular uncertainty sets right maybe have a nominal next state prediction and some sort of coordin ADV device uh next uh sort of uncertainty estimates for the next state you can basically reparameterize this Choice as sort of a new Dynamics Model F till there that in a way takes this control action that lets the agent control its luck into account okay and so now we can sort of keep unrolling this further get another optimistic next State uh unroll it further get another optimistic next state and then finally reach this High reward State and so what's nice about this is this is basically just a standard approximate dynamic programming problem to which one can apply standard policy gradient techniques in order to solve it right basically throw the hammer at it right that's kind of the Matrix right that's the S learned simulator inov that you have to optimize that inov incorporates the epistemic uncertainty about it okay so here's kind of a simple cartoon illustration on the uh sort of inverted pendulum task you want to swing up the pendulum so here's um start at the beginning right so the agent knows nothing about physics it only has some big smoothness assumptions about the Dynamics and so the first optimistic trajectory just believes if I just push it in One Direction I can swing it up and of course that's not how the world works right so if you actually carry out that policy the pendulum just swings down a little bit uh in the swung down position um then of course you update your model right you quickly learn that you actually have to swing back in One Direction in order to swing it up on the other direction um that still doesn't quite work but now you collect relevant data over a large part of the state space condition on it right now just a little later um basically you learned how to swing it up but it's slightly overshoots right because you haven't collected any data on the top and then just a little later it basically has found an optimal policy where sort of the optimistic estimate corresponds to the actual one uh observed okay and so now that's of course a very simple task you but apply this to kind of standard DL Benchmark tasks so here's comparisons against sort of this greedy the certainty equivalent method just averaging out over the uncertainty in the transition model also comparing against Thompson samplings or posterior sampling where you basically sample one of the models from the posterior and here's this optimistic exploration which already in this setting does maybe a little bit better there's of course a lot of variability um in uh when training these reinforcement learning agents they can make the problem Harder by increasing sort of action penalties right sort of to typically would want to encourage the agent to use as little Force as possible okay and so if you do that expiration actually becomes harder and why is that well standard policy gradients if you penalize using large force will essentially learn to do nothing right will basically learn to sort of stand still okay but with this reparameterization you learn that there is actually a chance of sort of getting further if you just stick with it right and eventually kind of you really need this optimistic in order to reach kind of spice reward States and here's just sort of a little uh Cartoon illustration of this right so if you don't use action penalties and just use POS gradian techniques you sort of just happen to learn how to move forward by vigorously waving with your extremities or so right so it's probably not expiration you would want to do in a real robot right and so uh if you kind of regularize by essentially controlling uh these action penalties then uh the behaviors uh are maybe a little bit more constraint okay but of course expiration becomes harder and you can do similar ideas once they have con straints as well so this was only about sort of f expiration you can also be a bit more careful about reasoning about constraint satisfaction uh so and for example when you can do the same idea sort of in constrain mdps which is one way of not just having a reward but also having a cost that may be quantifies the probability of visiting unsafe States right that's one formalism that's been uh looked at in this context where you uh want to uh sort of uh ensure uh constraint satisfaction and now again the trouble is that in standard constraint DPS you need to know what the transition model is so you maybe need to be able to sample from it okay but you can use that same kind of idea but now you can sort of be optimistic with respect to expiration but be pessimistic with respect to uh constraint satisfaction right and use similar kind of uh reparameterization ideas in a way to turn this into uh a problem to which one can apply sort of the hammer right uh standard uh deep uh RL um policy gradient based techniques for constraint uh policy optimization okay and so there are some experiments that we describe in the I clear paper on sort of learning to control I mean this is relatively simple uh robotics tasks in this uh safety gym uh Benchmark site sort of a point Mass some simple car model kind of a simple quadruped in order to solve tasks like reaching a goal uh touching buttons pushing a box to a goal uh and the likes from sort of first person VI so there a forward facing camera I tried to learn directly from uh from pixels um and now basically used kind of a stat based model uh to to account for the partial observability and sort of do uncertainty quantification around that um but otherwise the ideas are basically the same and you can apply them to the constraint uh setting as well right and here's kind of just examples of the types of observations that the agents get to see in the tasks that is going to carried out okay so there's a bunch of experiments where we compare um this algorithm this optimistic pessimistic uh algorithm um with bunch of other model based uh and model free uh de algorithms both along the final objective achieved at the task uh how kind of the fraction of cases where you achieve constraint satisfaction at the end of the task right where you satisfy really find a safe policy in the end as well as uh safety during training this is a very uh difficult task you really learned from scratch by observation only from pixels right so essentially safe expiration is very difficult in that sort of setting but at least you still want to kind of learn kind of as carefully as you can and so uh basically uh across this comparison uh this approach leads to good task completion um is the only one that's actually able to safely complete all these tasks uh and also does pretty well even not perfect in terms of kind of the safe exploration objective there's also comparisons against kind of model free techniques that are given a lot more environment interactions so orders of magnitude more environment interactions and you sort of find similar per performance in the end uh but of course much more efficiently in terms of much fewer interactions uh with the actual environment okay so that brings me to kind of the last uh part of the talk that I want to do in order to sort of com combine some of the ideas we've been discussing uh throughout this uh this presentation uh and so here's kind of a simple illustration on a task of taking this raced car and what you want to do is you basically want to reverse park it uh it's not the particularly complex task but it's sort of nice uh examp it's non-trivial in the sense that there's slippage right and there's kind of uh properties of the tires and so if you want to really carefully model this uh it's uh it's uh it's challenging and so we can first just run kind of this basic model based RL algorithm that I've described to you before and so uh after a couple episodes uh so initially just uh sort of learns collects data uh again it uses an completely uninformed Dynamics model so it knows nothing about physics right it knows nothing about the environment but after about 20 episodes I saw it roughly uh is able to learn the task right so so in certainly I mean model free R algorithms will use many orders of magnitude more samples in order to uh to get there so it's relatively sample efficient still to learn from completely from scratch of course maybe a much more standard way of trying to solve that problem right from controls engineering would be to maybe derive a first principle physics based model right do this ID to estimate the parameters then derive a feedback controller and deployed on the system right there maybe a much more sort of canonical stman that one should compare with right and so now um of course if you're careful with modeling this works perfect right if you use a very simple kind of model so here is basically you just use sort of a bicycle model and there is of course a simp to real Gap so you won't exactly sort of solve the task you'll get pretty close but you won't exactly solve it of course you could spend more work to kind of model tires model slippage and so on right um but instead we want to be more data driven and so the question we asked is kind of how can we get around sort of the simpal Gap kind of in a more data efficient uh fashion and so what we've been exploring is basically through this lens of basian meta learning um but instead of starting with a data sets that's collected from a bunch of related tasks we want to replace it with a simulator okay so let's just use a very simple first principle simulator right just the bicycle model with a bunch of sort of different parameters and in a way describe the underlying stochastic process the prior assumptions of the Dynamics model through in a way prior distribution over these different parameters that govern the simulator okay and so now what you want to do is you basically want to learn a model sort of in a completely data driven fashion however bias it toward and regularize it towards behaving sort of like the simulator does that's sort of the idea so we want inate to kind of com distill right the simulator into annual Network prior that's sort of the the idea behind it okay and so what you're basically going to do is use the same idea that we discussed before we to essentially estimate the score of this underlying stochastic process but now purely based on simulations and if you do this then already in sort of the first episode you at least get sort of close to the Target okay um and then after still get sort of at least going in the right direction and after about 10 episodes now it has solved the task uh refines it a little bit further during the next episodes and so on so it already starts off much better right much more informed by the underlying physics uh even though the model is just in your nwork Ensemble right it's basically just a fully dat model all we do is we sort of angiz it a little bit towards behaving like what the simulator does so here's some quantitative comparisons so um what this plot shows is negative log likelihood on uh a holdout data set collected on Hardware to kind of predict next States basically uh from current stat and actions and we compare on one ins side an approach that basically just does system identification just fit the model to data um on the simulator and then evaluating that predictive model on the hold out data here's kind of the uh uninformed um neural network model right that I had discussed uh before right where uh initially of course you start off much worse but eventually you find better Solutions uh another natural thing one can do is to sort of do a graybox model they basically kind of estimate and physics informed maybe mean and then sort of learn some Dynamic some deviations from it using neural network and that already starts off at a better place but in event converges sort of to the same solution and here's this uh approach that's fully data driven uh and just regularized uh towards this neural network prior not only does it start off at a better spots but it also converges to a better solution in the end and here's the experiment of that sort of is a quantitative version of the videos I showed before the XX is the number of episodes to carry out and the YX is basically the achiev performance at the task and the green one is basically um basically this modelbased RL from scratch with an uninformed prior knowing nothing about physics and the blue one is basically that's sort of regularized towards this bicycle model right it's this very simple uh first principle model right and again it starts off at a better spot and more quickly sort of finds uh finds a good solution okay so that's basically what I wanted uh to uh to talk about right so I think there's a lot of uh exciting open uh challenges uh still to be solved uh then trying to develop agents that can really safely and uh efficiently learn uh by interacting uh with the real world and I think one of the key challenges in this domain is really enabling models to learn uh to know what they not what they don't know right I think that's also one of the big Frontiers currently in sort of foundation models right how can one in a way uh figure out kind of the boundary of word alert representations still are meaningful and where they are not and I think that's sort of a key challenge to work on and so for uh one thing I just want to briefly mention uh so in Switzerland our next Generation uh supercomputer Logano is uh coming online in the future uh in the near future uh with the sizable number of top of line gpus and so we much hoping uh to get a chance to try out some of these ideas and scale them uh to uh larger scale uh settings in this context so here's just uh some of the references in case you want to read uh up more I guess it's being recorded uh and I'd like to end by thanking all the collaborators uh who uh who I got a chance to work with in this research thank the funding agen and thank all of you for your attention thank you very much okay thank you for the talk I'm curious what your thoughts are on maybe an alternative approach in imitation learning would you argue that we lose sort of the safety guarantees that you're you're presenting here or I'm just curious you're take no so um so imitation um so let one potential fundamental limitation in limitation learning of course you can always only do as well as sort of what you get to see right now of course there various different ways of starting with imitating something right and then sort of trying to improve upon that right there's various different ways of doing so and I think that's probably exactly the way to to go right and in some sense um yeah I think there's lots of ways of interfacing this right so for example when can sort of using imitation learning try to find an informed sort of latent space and then apply all the methods on kind of that informed latent space as opposed to maybe in kind of a generic uh State space so that's one potential uh potential way to go um I think there's many other uh approaches to interface it I think it's quite complimentary oh yeah please like the situation in which you waiting the like learning from the simulator like is it possible to take it to a more granular level like the action space like in the case of like the pendum we have like one action right but like in like the case of like the multi- robot like each of those Motors would like represent a separate action so is it possible to like like maybe like learn like the uncertainty for each of those like individual like actions that you could take and then possibly wait them separately like rather than like waiting the entire learning from the simulator as like a separate that's a good question so basically um so now if you have multiple actions right of course in first you can just sort of feed them into some black box that predicts kind of what's going to happen right and apply the approach as is right question is maybe is there some more fine granular ve to go about this and so we've going a little bit in this dire so so one way to sort of think about this in a way is to the lens of sort of causal Discovery right where you really try to kind of think about causal effects of individual actions you might apply and how they might in a localized fashion affect the state space and we've been doing some work in sort of model based cation optimization that sort of goes in this direction I think that's lot more to do in the space thanks a lot for the question any other questions then let's go for pizza okay yeah then thank you very much again 