# Sergey Levine-Talk Title: Safety in Reinforcement Learning by Leveraging Offline Data.

- break afterwards so I would like to announce the the next speaker is currently an associate professor in the department of electrical engineering computer science at UC Berkeley Professor Levin is focusing on machine learning for decision making and control with an emphasis on deep learning and reinforcement learning algorithms applications of his work include autonomous robots and vehicles as well as applications in other decision-making domains his research includes developing algorithms for end-to-end learning of deep neural network policies that combine perception and control scalable algorithms for inverse reinforcement learning deep reinforcement learning algorithms and more it has a PhD from Stanford University and since 2016 he is part of the electrical engineering and computer science department at University of California Berkeley so Professor Levin plays uh stage is yours great thank you um all right hopefully everyone can hear me I'll get started I'm going to talk about uh safety and reinforcement learning by leveraging offline data so um as many of you probably know reinforcement learning can provide a a very powerful framework for automating acquisition of behaviors on acquisition of skills including safety critical skills so we can use reinforcement learning to learn policies for things like robotic Locomotion sidewalk driving flying quad rotors and so on and that's very promising because if these policies can effectively use Rich sensory perception if they can develop a rich repertoire responses to whatever situation they might encounter then they'll be very effective in safety critical systems is synthetics however um on the on the path to these Solutions reinforcement learning algorithm need to explore so as a little uh illustrative example let's say we have this uh walking robot and after 2000 iterations it learned to walk very reliably on uh you know this infinitely large flat plane um and it walks without falling for a very very long time however during the training process it needs to repeatedly experience failures including catastrophic failures so that it learns essentially not to do that and of course in simulation that's fine because we're pretty happy with the final policy that we get but if this were running on a real humanoid robot training in the real world then all those failures on the path to successful policy would probably destroy the robot and that it wouldn't matter how hard to walk because it would be gone so that's basically the challenge and if we think about uh safety and uh generalization particularly on in unseen settings I think it's probably fair to say that in the world of supervised learning even though the problem is not fully solved it's much better understood whereas in reinforcement learning there's some pretty major conceptual challenges so in super for supervised Learning Systems we kind of know that if everything works as intended then you should get decent generalization to in distribution inputs meaning that when your test inputs are from the same distribution as your training inputs no it's essentially your validation error kind of reflects what you expect to happen but supervised Learning Systems May perform unpredictably on out of distribution inputs so they're reliable for in distribution unreliable Auto distribution and even though the problem is far from solved it's not nearly as bad as RL because we know that as long as we can avoid out of distribution inputs for example by carefully choosing our training set or by having some sort of uncertainty monitoring will be in decent shape uh so not not to you know understand the difficulty of that problem but it feels like something that there's at least a little bit of a handle on when it comes to reinforcement learning um the trouble is that just acquiring a good policy already requires potentially unsafe exploration excuse me so there's in some sense kind of no way out of that and uh trying to incorporate uh prior data let's say you had some prior data that tells you how to be safe foreign that requires you to have some distributional shift pardon me because presumably if you're adding prior data you want to do better than whatever happened in your prior data which means the distributional shift does not just an undesirable artifact It's actually an unavoidable consequence I've tried to do well at the tasks Okay so important so if we can reduce the risks NRL to familiar indistributional system generalization risks like the kind in the left hand column then we will be much closer to methods that are actually viable for the real world so the trouble is that in reinforcement learning you have to have unsafe exploration well you have to have some kind of exploration which is potentially unsafe and you have to incur at least some distributional shift because you want to do better than whatever you've seen before and that's basically what Improvement is but maybe there's some way to kind of get that underground control and get those risks to look more similar to the risks that we're familiar with okay that was a little vague um so let me try to make this a little more precise this is the question for the stock can we develop data driven RL methods so um in traditional on policy RL we have an RL algorithm that interacts with the world uh collects a little bit of experience improves from that experience and then um hopefully does a little bit better as a result of that in offline reinforcement learning where we incorporate prior data the idea is that we take some existing prior data set we're going to use that prior data set to train up some policy that does as well as it possibly can using only that prior data and then we would deploy it and hopefully we'll do something decent and of course in reality uh we might still fine tune it with additional online training after that and you know what one of the big Promises of offline reinforcement learning outside of all the safety stuff is that conceivably we could take let's say a large data set from all the past tasks that the robot has ever solved before use that to initialize some kind of policy that achieves some decent initial level of performance deployed and that would already be way better than starting from scratch would avoid the bulk of that nasty exploration like the robot you know the walking robot wouldn't have to discover how to not fall but then there would be some additional fine tuning to really get performance up to the highest level uh possible okay so that's kind of just offline RL in general without any kind of safety element in it um but you know that already gets us uh you know a lot of mileage because if we can use prior data to overcome some of that exploration challenge we can enable RL in many more settings but let's talk specifically about the state so um we could train with offline our own but our goal here will not just be to like flat out solve a task using prior data because that might be true ambitious in some cases it'll be to provide an initialization for safely fine-tuning the current task which would be done with online interactions and there are kind of two ways that we can frame this problem statement we can say we want task agnostic pre-training so use prior data to learn how to be safe in some very general sense and then use online fine-tuning to learn a particular task or you could have test specific pre-training maybe use your prior data to figure out potential solutions for a task and then do some very efficient and hopefully safe online fine-tuning to find which of those Solutions is the best one and then you do some online adaptation okay um so that's basically the idea we're going to adapt online and explore for the new task while using something that we pre-trained for prior data to provide some kind of uh constraint or something to avoid those nasty failure cases um so there are two approaches to this that I'm going to cover in today's talk the first is going to be that that task agnostic problem statement so this is where we use prior data to learn some general purpose constraint that we will then use for some new Downstream tasks with online exploration and then I'm going to talk about a task-specific approach where we would actually use data for a particular task and learn a space of potential Solutions and then select among those potential Solutions with online exploration so let's start with the first one um so here we're going to be learning a general safety constraint from prior data that is agnostic for the task and then we can use it for for online data collection for whatever task we want so we're going to have some prior data and that prior data can come from many different places it could come from hand designed policies it could come from a previous controller that we ran on our system it could even come from random data collection so you can basically think of this as like whatever your system did before somebody logged it and that's what you're going to start with and we're actually not going to require this prior data to actually all illustrate safe behaviors all it's supposed to do is just provide data that illustrates how the system works it may contain unsafe behaviors or it might not now uh conventionally if you were in the situation you might for example use the priority data to learn some model of your system like a Dynamics model and then you know if you want to do model based RL you could say well let's use that model to control the system and you know if that's a good model it'll it'll work well and then the big question you're concerned with it is the model correct not is it correct on the training data distribution but is it correct in the kinds of situations in which it will be used at test time which will of course differ from the prior data because you're trying to accomplish some particular task and that's a particularly delicate problem so coming back to that distributional shift and supervised learning comment from before I can make this a little more concrete you could say well if you train your model using empirical risk minimization kind of the classic supervised learning framework uh then what you really did is you minimized some loss let's say like a squared error regression loss on your training data distribution and then if you're asking is your model correct you're really asking if I give some new input to the model will the model make a correct prediction for that new input and that's actually a very difficult question to answer so uh one of the few things we could say is that if you evaluate the average loss under the same distribution as the one the model was trained on and the model didn't overfit then you would expect the average loss to be low but in general you wouldn't expect the loss to be low if you're testing under some new distribution in this case P bar of X which might be different than P of X so that's basically distributional shift and distributional shift is going to happen if you don't avoid it somehow because you're trying to solve some new tests that's different from the old tasks which means you'll probably go to different states and in those States they might be unfamiliar to your model okay so that's essentially the problem we're trying to address and there's something kind of funny that happened here I went from talking about safety to talking about distributional shift and why did I do that that's that's not a very conventional way to think about things uh because typically people think of safety in terms of constraints but I think it's actually very appropriate here because essentially if our model is correct then any method that uses that model would be able to predict that something unsafe might happen so we don't in some sense need to do anything special for safety if we don't have distribution because in that case your regular RL will already understand that this particular behavior is undesirable because the model is accurate so the thing that we're really concerned about is mistakes in in the model not so much figuring out which states are safe or not because if the model is accurate you can figure it out okay so learn models can make mistakes when there's too much distributional shift so one thing we could try to do is use this prior data to figure out a constraint that will avoid distributional shift and as long as that works then the model will be accurate and then the model can do its own job and avoid going into unsafe situations so can we learn constraints from prior data that prevent distributional shift okay so that's what we're going to do we're going to use prior data to acquire some object which is going to be this distributional shift constraint and formally that object is going to be a function G of s comma a so it's a function of state and action and that function uh will satisfy some property that says that as long as it's above Epsilon if you take that action you will remain in distribution and I'll Define that more precisely in a second and then for some Downstream tasks so we're actually interacting with the world will only permit actions for which GSA is less than or equal to some threshold so and again I just want to emphasize here that I'm using the word safe a little bit unconventionally here I'm using it to mean guaranteed to stay in distribution not necessarily guaranteed to like not fall or something like that and that's under the assumption that as long as you stay in distribution the model itself can take care of not falling down or whatever other constraint you have okay so what should this constraint do we only allow an action a t when uh G of sd80 is less than or equal to Delta and in particular what we want is we want for for all future time steps T Prime greater than or equal to T we want the log probability of the state in action at that time step to be greater than or equal to Epsilon why do we want this well um this is more or less saying that if you take action a t then for all the time steps in the future you will never encounter a low probability State action low probability here means low probability under the training distribution now this doesn't actually guarantee that you don't make mistakes because in general uh the loss is only low guaranteed to be low in expectation but in some sense it's kind of the best we can do so uh it's uh it's a it's a little bit of an assumption that as long as the density is not too low your probability of making a mistake will be small although in general that's actually pretty hard to guarantee so there's a little bit of a of an assumption being nature but it's kind of sensible it's saying like you know you're not going to go into regions that are too unlikely and therefore everything will be fine okay so here is like a a very simple straw man method we could think of for getting us started what if we just only allow actions for which the log probability of sdat right now is greater than or equal to Epsilon basically does avoiding low probability actions ensure that you avoid low probability states in the future because if it does then this would give us a very very simple method for solving this problem unfortunately that doesn't really work and the reason it doesn't really work is because in the training data you might have um multiple different actions in very similar States so in general you might have this fragmentation of your probability Mass so what the animation on the right is illustrating is that at that first time step you have two actions available up or down and if you take the down action it has 50 probability all the subsequent States and actions also have that same slice of the probability Mass but if you go up there are multiple options available to you in the data this is in the training date not necessarily in the in the entire MTP you have multiple options available to you and therefore your probability mass will keep on splitting and splitting and splitting which means that even though that first time you took the up action it had a probability of 50 by the time you get fired up to the right you'll have much lower probability for all the options available to you so that's a little it's a little maybe counter-intuitive that even though you you might always take the highest probability option after that entering that top Branch nonetheless the probability of all the options will continue to decrease so therefore it's not enough to Simply restrict yourself to high probability actions you need to actually consider the probability of future States and actions that you will enter after taking that action okay so what we're going to do in this work is we're going to draw a connection between staying in distribution and lapan of stability so many of you probably already know this but here's like a a quick 30 second primer on what network of stability is so stability in general says that if you start at a location that is within some ball of radius Delta then for all future time you will stay within some ball of radius Epsilon and so that this is an illustration of that so you start within the smaller dotted ball inside of radius Delta if the system is stable you will stay within some bounded range Epsilon and for Simplicity you could also Imagine The Delta and Epsilon are the same so if you start within a ball you stay within that ball that's a simplification but that also is a reasonable intuition so if you have a stable system then you can construct a laptop function V of X such that it's it defines a potential field that decreases along the Dynamics so what that means is that the value of B of X should decrease as you run your system or at least not increase so if such adopting a function exists then it's a stable system and I'm sweeping a lot under the rug these are not of course technical statements these are this kind of intuition more related to what we're doing is a controlled openoff function and a controlled dependent function is kind of the same idea but for a control system and a controlled up with a function basically says that um if such a function exists then your control system can be stabilized with some controller so in particular it means that you will remain stable if the control U is chosen such that the dot product between the gradient of V of X with respect to X and the Dynamics function f of x u for the U that you chose is negative which basically means that b of X will not increase as you run your controller um now this is a lot like a value function and reinforcement learning and in fact the connection is more than just superficial it can be made precise essentially uh if formulated in a careful way value functions can be laptop functions and the key idea that we're going to explore of course we don't want to stabilize our system around some Epsilon ball near x equals zero we want to stabilize it in high probability regions so instead of using lapinov functions to stabilize around x equals zero we're going to extend the concept to stabilize in regions with high density which might no longer be a ball it might not no longer be convex so a lot of things get more complicated but similar kind of ideas could be used so essentially in the same way that laptop stability ensures that for all future States the norm of X is less than Epsilon here is going to ensure that uh for all future time steps the log probability at that time step is greater than or equal to Epsilon okay so let's come back to our safety constraint so that's this G thing G of so we're only going to allow an action when G of s d a t is less than or equal to Delta and the intuition here then this is like somewhat in formal intuition is we're going to want to make G look a lot like the negative log probability but we're going to make it uh in in some informal sense a little bit like leopardov consistent meaning that it will guarantee that not only is the log probability high now but that it's going to be high in the future which is a little bit like that property from before where a conventional function guarantees that if your Norm is low now it'll be low in the future so we call this thing uh Aleppo density model because it combines ideas from density modeling uh namely that it's going to look a little bit like log p with ideas from that enough functions which is its ability to stabilize in some region so this is the definition of an open up density model so it's defined as this function G where if you take actions for which G is less than or equal to Delta then for all future time steps your log probability greater than or equal to Epsilon for any controller as long as it obeys this constraint so you're assuming that you will continue to obey the constraint in the future okay now there are a number of important questions we could ask about these ldms first can we actually learn them from offline data so we got on this whole track because we wanted to learn safety constraints from data can we actually learn ldms can we guarantee that using ldms as constraints actually guarantees bounded distributional shift especially under realistic conditions when you have sampling error approximation error and so on and can we use ldms in conjunction with Downstream methods like model based RL um so the answer to the first question is yes and in fact the algorithm that we get out of it ends up looking a lot like more conventional offline reinforcement learning methods based on value functions but the second question the answer is yes and we can even account for sampling error we do this analysis and for the third question the answer is yes and uh we can actually show them empirically that using ldms with model based RL leads to a reduction in catastrophic failure rates so let's start with how we can learn ldms from data so I can take this condition that basically the definition of the ldm and I could write it down symbolically here in this equation so what I want is to satisfy the condition that G for some state in action which without loss of generality I'll write at time step 0 so G of s 0 a 0 should be the minimum over all the actions of the maximum over all future time steps of the negative log probability so it's essentially the least probable thing that you will see in the future as long as you act to avoid the least probable thing that's basically what it is and one of the things we can do with this equation is we can express it recursively so the expect the expression here is maybe a little cryptic but if we express it recursively I think it actually becomes more intuitive so we could say that g is the maximum over negative log P now and G in the future where we're going to take the best action to uh uh to maximize log P so the signs are maybe are a little confusing because to um to sort of use a conventional consistent control theory where we're going to keep G low but we want to keep log P high so it's negative log B right so we're going to keep negative log kilo um so what this expression basically says is take the maximum over the current negative log p and the future negative log p and the future negative log p is captured by G at the next time step where the way you got to the next time step was by taking the uh the best possible action and um those of you that are familiar with reinforcement learning would recognize this as being quite similar to a bell and backup right so Bell and backup is also recursion over time where you have something like a current reward and a future value here instead of adding the current reward to the future value we're taking a Max so we're taking the larger of the two because we're concerned with the largest value of negative log P that you will see for all time steps rather than the cumulative sum but because it looks so much like a Bellman backup we can actually train it with an objective that looks like a value functional learning objective and there's a small typo here that should be squared but essentially the loss function you can use is just the difference between the left hand side and the right hand side of this equation squared and that'll be a valid way of training uh a function approximator to represent the depth enough density model so our algorithm will essentially be based on this so the ldm training procedure just to summarize at a high level will be first train a density model train a model of p on all of your prior data to train this G and I I use a subscript Phi now to denote that g now is a function approximated with parameters Phi such as a neural network for example so train G5 using any offline RL method uh we're going to use a variant of conservative Q learning but any good offline enroll method will do the job and the core of that method will be this modified Bellman backup so the rest of the offline are algorithm works exactly the same as any other offline RL algorithm except the Bellman backup instead of having a sum of a reward to the next value has this Max and that's really the only modification okay uh so that's how we train ldms now let's talk about how they might be used uh so our ldm now is going to be this function approximator basically a neural network that takes in sna and outputs a real number and maybe we're going to be doing model based RL that's not the only way to use ldms but one of the ways we can use them is with model based RL in which case we have a learned model FSA which takes an essay and predicts the next state so we're going to use our prior data use it to learn a safety constraint also use it to learn a model and then try to perform the downstream task by for example doing model creative control with that model using the safety constraint to constrain the actions that we allow ourselves to take so we only allow actions a when GSA is less than or equal to Delta so we can write this for example as uh in the form that's maybe more familiar for model predictive control every time step we're going to plan a sequence of actions A1 through H for some Horizon h to maximize the rewards subject to the constraint produced by our Dynamics meaning the next state is f of s t a t and then we have this additional constraint that G5 sdat should always be less than or equal to some constant okay and what we're going to compare this to is an approach where instead of using the ldm we'll just use the density model so in some sense kind of that's that's sort of the default thing based on what exists today just only allow high probability actions and the question that we're going to be interested in answering is if we use the ldm to constrain our actions instead of using a density model will we actually do better okay now this is I just want to emphasize that this is not the only way to use ldms but it does provide a reasonable default way uh to use them with model based URL that we can analyze just get a sense of whether the ldms are doing a good job of uh satisfying those constraints okay um so we're going to compare the ldm to this kind of density constraint first theoretically and then empirically so let's talk about the theoretical stuff for the density constraint we can construct a counter example where this method is arbitrarily bad and that counter example is basically the branching scenario from before essentially you can construct scenarios where in some states all the actions have equal probability but some of them lead to branches where reduction in probability is unavoidable and in those cases you can you can basically get this thing to blow up exponentially fast so the full counter example is described in the paper but uh basically the short version is if you just use the density as a constraint there are counter example data distributions that are arbitrarily bad for this arbitrarily bad in the sense that your constraint will be violated and it'll be violated very quickly the guarantee provided by ldms is essentially that if you pick a constant C then even in the presence of approximation error on sampling error your future probabilities will be lower bounded by some expression now because you're in current error they are not actually lower bounded by log C anymore like law if you have a perfect ldm they would be lower bounded by log C but if you have an imperfect ldm they'll be lower bounded by log C minus some error terms but critically all those error terms can be made small if you have low enough approximation error and enough data so let me just walk through what those error terms are so Epsilon P this is your error in fitting the density model essentially you're going to use some kind of density model to train the ldm and the density model is attempting to estimate the distribution in the training set and that estimate itself might be wrong so if it's wrong you incarcer some error but hopefully that error will be small this constant R essentially represents recoverability uh so it's a constant describing how easy it is to come back from low density regions if you if you live in kind of a an environment where you fall off cliffs a lot then of course this value will be bad um this uh K is the full task horizon or if you if you're in discount or setting it would be one over one minus gamma and this Epsilon LS is the TD learning error per iteration so when you're doing this temporal difference learning the Bellman backups you incur some error and that error is at Epsilon LS and that's that's kind of the the main source of error that you're worried about when trading the ldm um and Epsilon uh Finn this is the error and fitting the density at the last time step very similar to Epsilon P so the thing to note about this bound is that everything is basically multiplied by some kind of Epsilon so if your error and fitting the density model is low and your error and the TD backups is low then this quantity as a whole below and you know some more things you can note about it is that it increases with Horizon and it increases with this uh recoverability constant okay um let's talk about some empirical results so here we're going to be testing the ldm with this model based method so this is a model period of control with a learn model and the ldm as a constraint on a hopper control task so this is a one-legged Hopper that's hopping to different locations on the x-axis and the x-axis on the plots on the left represents different thresholds so as you go further to the right the threshold becomes more harsh so on the left you're permitting all actions on the right you are not permitting any actions except the safest one and the top plot shows the reward which in this case is is how well you reach the goal and the bottom plot shows the failure rate so these are not the same thing because you can avoid failure but yet still fail to reach the goal so if you use a very low threshold then uh the hopper Falls all the time so it has a high failure rate if you use a very high threshold then it basically stands still it doesn't fall but it doesn't reach the goal either and if you choose a more intermediate uh threshold then it will actually call up to the Target the blue curve is the LDL method the orange curve is kind of the main Baseline which is the density constraint and you can see that the density constraint does avoid unsafe actions once the constraint is very strong but by then its ability to actually reach the goal is much worse so we tested this across multiple different tasks Hopper lunar lander as well as a medical a simulated medical task for a glucose uh uh for insulin pump some glucose and in all these cases we found that the ldm did a better job of avoiding catastrophic failures and also a better job of uh actually getting a higher return which makes sense because if you get a catastrophic failure then you don't succeed at the task okay so the takeaways from this the openoff density models can be trained by offline RL from prior data and they provide a safety constraint that keeps Downstream controllers inside the data distribution in the same way that the up enough stability implies never exceeding a distance to the state to a stationary point the ldm implies never dropping below a desired density level and this can provide in-distribution safety to Downstream model-based controllers as well as other kinds of controllers that I didn't talk about like imitation learning and model free methods okay now let's talk uh a little bit more briefly maybe about test specific methods for pre-training so here's a a big question if you have prior data for the particular task that you want to solve why is it not sufficient to just train on the prior data right for ldms we have prior data for that was task agnostic but if we have prior data for the particular task you want why can't we just train on that and solve the task so what do we get from the online phase well we can think of a Bayesian perspective on this question we can say that well maybe if we have a data set we'll really learning is a kind of posterior from the data so the probability distribution over policies given the data set so it's a kind of distribution of a potential optimal policies the data might not fully disambiguate exactly how to solve the task optimally but it might narrow down our space of hypotheses so if the uncertainty in this distribution is high meaning that from looking at the data set you think many policies might might be optimal potentially then you need to use this posterior to explore and adapt online to figure out which exact policy from that posterior is really the optimal one so that's kind of interesting because it says that the role of this online adaptation is really to to resolve leftover uncertainty that we might have from an ambiguous training set and that's not really that profound of a statement because if your training set is empty then of course you have full uncertainty and then you're just doing online RL so there's a lot of uncertainty to resolve if your prior data set is very large you might have very little uncertainty to resolve but nonetheless we're going to take that perspective when exploring online try potentially optimal strategies and pick the best among those so here's how we can think about this we have a prior data and all that prior data comes from some true mdp right that's where it came from it came from an mdp but we don't know what that true MTP is and because the data set is limited for that finite size data set it might actually be compatible with many possible MVPs so there is a distribution or possible mdps P of M given t for which the data is compatible with all of them so then a Bayesian objective for using this data would be to maximize the return of your policy in expectation under the distribution over mdps that could be consistent with the state that's a very Bayesian way to think about offline RL essentially so you can kind of you can imagine the process under which this policy is training as follows you have a p of M given D and you're imagining that at the first time step one of the mdps from this distribution will be sampled and the policy will act in that mdp but it doesn't know which one it's acting in so it has to learn a behavior that will work well in all of them weighted by their probability the interesting thing is that at this point this is actually a partially observed Markov decision process because the policy doesn't know which MPP it's in so that's kind of interesting statement that when you have finite data to learn from you're actually trying to solve a palmdp even though the real task might be an MVP so the the finance of your data actually induces partial observability because it induces uncertainty Okay so what could we do with this well we could adopt a Bayes optimal strategy which is to solve the epistemic pump DP this pump DP where partial observability is induced by your limited knowledge to get a policy and then run that policy online uh and uh you can then minimize your um your regret when you do that or you can even adapt it further now the optimal probability solution in general is a memory based strategy so roughly speaking the optimal solution for in a pump DP would essentially be adaptive because it would look at its history use that history to figure out which mdp it's in and change its Behavior so if you can somehow actually get the optimal policy from the for the pump DP that policy will capture an adaptation procedure inside of itself so that means that when you run it online it will actually adapt so Pi is an adaptation or learning procedure if it is the optimal policy for the pump okay that seems pretty great like we can just solve the pump DP and get a solution why is this hard well solving pump DPS in general much less from offline data is actually a very difficult problem much harder than solving mdps and furthermore we don't even know what P of M given D is we have to learn that from the data and that's a much harder problem even the model based RL we're not learning one model we're learning a distribution of our models so it would be really nice if we can sidestep these two difficult problems it would be really nice if we could avoid having to represent a full mdp posterior and it would be very nice if we could avoid having to solve complicated pumpkin piece okay so let's try to avoid these things so here's the first idea the epistemic pump DP is not just any pump DP it has some special structure so when we solve the pump DP to get Pi the special structure will actually allow us to restrict the kinds of policies that we have to consider and that'll be very convenient we don't need an arbitrary memory we all need to track the posterior belief over the mdp so essentially we have a prior belief P of M given D and as we collect more experience with our policy that belief will change so it'll be P of M given the union of D and the online experience and if we assume that we're adapting within a single trajectory it's really P of M given D combined with all the times that we've seen so far for the current trajectory and we're going to call that belief B and subscript T because it's changes with every type of stuff so B is just some Vector of numbers that represents your belief over which mdp you're in the optimal pump DP policy Pi is markovian given B so if you have B you don't need any other memory so we really only need two things we need to be able to represent and estimate B and we need to be able to train a policy condition on B so that it's optimal for all possible bees but this policy no longer needs memory all it needs is B so we're going to learn the policy with awful NRL using random B's and then we'll uh represent an estimate B that's still hard so that's what we're going to talk about next so what are these beliefs well in general the beliefs need to parametrize our belief you know our distribution over mdps the interesting thing here is that a posterior over Q functions actually captures the posterior over mdps like everything you need to know about the mdp is actually captured inside the Q function so two mdps are different in a relevant way only if they lead to different Q functions if they don't lead to different Q functions those differences are irrelevant to your policy so that means that if B is sufficient to capture the distribution over Q functions induced by the mdp that's actually all you need and that's a really significant change because now we don't need to represent distributions over Dynamics or rewards all we need to do is represent distributions over Q functions and that will be sufficient so we need to represent P of Q given d and we need to update it each time step to get an updated posterior over Q the way we're going to represent a distribution over Q functions is by using a bootstrap Ensemble now that's a very coarse approximation it's basically saying I'm going to approximate the distribution over Q with a mixture of direct Delta functions but it's a very simple way to build the approximation because training bootstrapped ensembles for Q functions is a very well studied topic so essentially the way you do it is just you just resample your data set and train independent neural net value functions on each resample data set and then the way we're going to update our belief is by changing the weights on The Ensemble members so instead of just averaging Over The Ensemble numbers we'll do a weighted average and the weights will represent our belief so essentially the member of The Ensemble that seems to be agreeing most of what we're seeing will get higher weights the others will get lower weights and will adapt so now B is just a vector of numbers one number for every member of The Ensemble that represents the weight on that member of The Ensemble so that's pretty convenient now B is a fairly small Vector if you have 16 models in your Ensemble B is just a vector with 16 numbers now there are a few important details in instantiating this one of them is that the Q function actually also depends on B so you need to actually feed B into your Q function because even though each member of The Ensemble is separate they're not just estimating the optimal behavior for themselves they're up to their estimating the optimal behavior for any belief that's that's a little subtle like your policy needs to get BS input and each of your Ensemble members also needs to get the BS input because the optimal Behavior will change depending on your beliefs so the ensembles are all update on their own targets but for beliefs that might say that other targets are actually correct so during offline training the queue function depends on B and B actually changes each time step because a test time will be updating it so we have some Dynamics on B so to train for every sampled mini Dash we're going to pick B randomly and then we're going to update to B Prime according to belief Dynamics so the belief has Dynamics which means that every time you observe a transition B changes and there's actually a little bit of a design choice in deciding what those Dynamics should be but you know formally what they should be is this V Prime should be proportional to B times the probability of The observed transition given uh the corresponding value function so what likelihood do we use for this probability well a very natural choice is to use the Bellman error as a log probability which is basically saying that your values are normally distributed uh or distribute a particle LaPlace distribution in this case with the mode given by the value function itself this is actually a choice so that's not the only choice you can make but it's a very reasonable Choice it's just saying that you know the transition has high probability under your value function if the value function is low Bellman error for that transition and then you can use this to induce Dynamics using the equation uh in the lower line so it's essentially using development error as a kind of pseudo likelihood so this is the full algorithm pseudocode I'm not going to go through it in detail it's essentially what I said before but the important steps are during a value function training we sampled the current B randomly compute the next B Prime according to these Dynamics and then optimize with again with any offline RL method and then we get a policy and a q function that are both conditioned on B and then we run the policy at test time updating B according to these same belief Dynamics as we go which gradually Narrows down your belief over the mdp over the course of a trial so this method works it uh outperforms albeit a somewhat narrowly uh prior uh methods including methods based on ensembles on some Benchmark tasks and uh in particular the performance of the Adaptive policy shown as a dotted blue line here at the top is significantly better than the performance of any one member of The Ensemble so the the curved line each of those points is a different number The Ensemble sorted from best to worse and you can see that the Adaptive strategy is actually better than the best Ensemble member which means that if we're adapting the belief we're doing better than just picking one of the Q functions uh we constructed a toy task that is meant to specifically uh elicit adaptive strategies so here this is essentially an image classification task embedded inside an mdp where you get a picture from cfar of a boat a plane a car or something else and you have to go to a corresponding door so if you misclassified the picture you get to change your mind and try it different or but of course that's never the optimal strategy for any single mdp so you need to be aware of your epistemic belief to solve this and that actually ends up working very well with the strategy okay so I'm running about out of time so I'll conclude the key takeaways training on offline data implicitly induces partial observability which provides for principal motivation for online adaptation we can represent a posterior over Q functions with an ensemble and condition the policy on the belief and future work would improve on this further by developing better methods to represent the posterior and maybe better methods to adapt okay so I talked about how we can use prior data to provide some degree of safety I'd like to thank all of you for listening I'd like to thank the students that carried out this research and if we have a few questions uh if you if you have a few minutes we can take questions thank you very much for the talk so maybe we can start with some questions from the panelists maybe organizers yeah I just have a question Professor so in the definition of the uh like a lot of functions and the Bellman equations the task was defined as an infinite Horizon and uh like uh for the continuing tasks and I wonder that how the if the task is a finite Horizon episodic test what is the final uh constraint that we need to put on the G function and it can be any arbitrary one or yeah thank you that's a good question I I actually swept under the rug of this difference but let me go back to the um to the result here so in the paper we actually derived this for both finite and infant um Horizons and in the bound actually the analysis I presenting here is actually for a finite Horizon that's why there's this K final and the only boundary condition is that you need the density model to have bounded error on the last time step as well and that's Epsilon fin now you typically you'd expect that Epsilon fin to be the same as Epsilon P but it's handled separately for exactly the reasoning stated we can also have a discounted infinite Horizon formulation that's a little bit more complex to write out but it works more or less exactly as you would expect the Horizon turns into one over y minus gamma and that derivation is in the paper okay thank you do we have some other questions from the audience from the other buttons so maybe one question for me I'm not sure if it's really aligning but in terms of like seem to real transfer or continue learning where there is explicitly like distributional shift from the training and the testing or the task itself can change over time or the distribution do you see that like this kind of methods can handle this distributional shift or you want to identify the distributional shift and then to say that you cannot operate anymore in some specific task where the shift is too big I think I think Professor Levin's up here sorry about that I had a connection hiccup could you repeat your question so in terms of symmetry or continual learning where we assume that there will be a distributional shift from what you have trained on and what you test on do you see this these methods can handle this situation or more hear the purposes to identify this shift and for example if you want to induce safe Behavior just to stop acting in such situations got it um that's a good question I think that for the um for the ldm method that I described it's probably not a good fit for that because it makes the assumption that you can avoid distributional shift meaning that you start in some State that's pretty familiar and then all you have to do is avoid going into unfamiliar places but with some with a with a transfer learning thing like simulation to real world transfer you would start off potentially an unfamiliar situation right away so you need there to be something gradual and in particular the ldm framework is not really accounting for changes in the transition Dynamics it assumes that you're still in the same mdp and you just need to avoid certain States I think if you want to do something like what you're describing I think you would need to combine ldms with some additional mechanism for measuring uh epistemic uncertainty if you're basically measuring that things aren't working the same way that they worked before even though you're in a similar State and that will be an additional mechanism yeah thank you and we have other questions uh yeah I have a question as well so uh I was working I was wondering about the prior data is it normally how difficult is is it to acquire these data and uh what if uh what about some applications like uh helicopter flight that's normally we cannot get them from anyone um how difficult is it in those days yeah yeah that's a great question I I think it's um it's very dependent on your domain of course so in some domains uh maybe you can get a person to provide some some training data if a person knows how to do the task in some cases you could also use data from past experiments so maybe uh it's difficult to get a system that satisfies all of your uh constraints right away but if you have to get started somewhere maybe you have a few trials where there's some failures and you'd like to just have fewer failures later on so it's domain dependent in many cases in robotics we can actually get data from Human operators to get the process started but uh you know in other cases like for example uh for a lot of industrial processes and operations research type problems maybe there's prior data from some hand designed control system that could be used to bootstrap the process let's continue okay I have a question from uh and threads page 26 said um MTP header corresponding code what if the data in this MTP is connected from a different policy um yeah yeah so uh the um the Q function can be trained on data from any policy so you can have a Q function for a particular objective for a particular policy and since uh Q functions like fit Accu iteration and Q learning are off policy algorithms they can be trained on data that came from different policies uh you have to be a little careful to make sure that uh again you account for distributional shift when you do that and modern offline RL methods do have mechanisms for taking care of that but other than that that's not really a problem okay thanks okay thank you very much I think that's all the questions and you're running out of time so uh I think now we can continue with the next speaker thank you