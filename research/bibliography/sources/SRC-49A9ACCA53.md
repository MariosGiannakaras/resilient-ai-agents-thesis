> Source: https://www.youtube.com/watch?v=Ph34wsFKeHo

hello everyone welcome to join for joining us so today we invite you to be holding to give us our talk about the safe reinvestment learning in the printing server now stationary and algorithms but yahodi is currently there are fifth year PhD students at the UC Barkley operations research experiment it's a research interesting called the reinforcement learning controversary optimization statistic learning during his PhD he focuses on non-stationary security system making problems such as temporary optimization the global convergence or pro-segration methods and the non-stationary refreshment learning hi hi yeah and you can start your talk oh thanks so much so uh can everyone see my screen yes oh great uh okay firstly I would like uh sending for the room welcome and it's my great honor to have this opportunity to present our recent work on the safe reinforcement learning in the presence of the non-stationarity yes so uh you know during the presentation uh please feel free to you know interrupt me if you have any question uh so uh so for today's presentation I will first uh start from the motivation and introduction of why we are interested in the safe RL under a non-stationary environment and then I will talk about our recent work on the three different uh scenarios for uh for these problems which are the non-stationary constraint mdp the non-stationary risk risk and sensitive control and The Meta safe reinforcement today and then if we have time I will talk about some uh future directions and the future ideas for the future projects okay so reinforcement learning has been one of the hottest Topic in AI by demonstrating its uh capability on attending the human level performance in video games beating the champion gold player and more recently uh discovering the novel efficient and provably correct algorithm for the fundamental tasks such as matrix multiplication however unlike this uh simulation based system such as video game or go in many applications of autonomous systems the existing reinforcement learning techniques are not yet applicable or are too risky to deploy due to the safety requirements for example The Collection avoidance in the self-driving cars are the resilience requirement of the power Network the robustness of thematical treatment as well as the risk management in the financial Market and in addition to this safety assurance the underlying environment May undergo changes and be announced stationary such as electricity demand in the power system and the autocorrelation of the daily returns for s p 500. it is also mentioned in the seminar book by the Richardson with regard to the father of RL that now station non-stationarity is the case most commonly encountered in the reinforcement learning and our project our project is motivated by this pricing need to provide computational tools and analytic Assurance for the reinforcement learning in the non-stationary environments under the safety constraints and in particular we will considered the interaction of the agent with a non-stationary environment which provides not only the reward signal but also the feedback on the safety requirement the safety constraint feedback will require the agent to avoid the better States or the bad actions while doing the exploration in addition due to the time very nature this reward and safety constraint signal may also be temporary and therefore the agent is not only unaware of what is bad ahead of time but also needs to keep updating their beliefs on the safety requirement because what is bad is also varying over the time and in this presentation we will consider two types of unknown stationarity the first one has the non-stationarity within a single task such as designing a controller to distribute and regulate the power Network whereas electricity low demand is temporary the existing algorithm designed for the stationary environments cannot learn very effectively this motivates us to design algorithms which can incorporate the detection or prediction of the future now stationarity into the decision making the second one considers the multitask learning with clear task boundaries such as low robot manipulator which tries to continuously continually learn to serve the T move the box or cleans a house where satisfying the safety constraints with human in the loop due to the similarity of these tasks the learning process should be significantly accelerated if the Rope if the robot manipulator can Leverage The Prayer experience for the new coming task this motivates us to design a mechanism to adaptively update the safe policy initialization so that each new task can be learned very effectively from the given initial initial policy as uh and in some races project aims to develop a set of computational tools with a rigorous analysis for the design of safe RL in the presence of non-station narrative in particular we were considered as a non-stationary constraint mdp now stationary risk consists risk sensitive RL and also the meta safe reinforcement learning and this table with the color coded Max indicate the extent to which the existing methods address the corresponding issues and compared with the relevant research directions our work is only one which addressed all aspects of the difficulties such as the multitask learning non-stationary environment safety requirement and theoretical guarantees in the non-station receive aisle now I will start with the details of the projects we get towards the realization of the non-stationary safe IL the first formulation we consider is the constraint mdp in the constraint MVP we can see that an absolute exciting will add to the beginning of each episode the age interface recite to a initial State S1 and takes an action A1 receives the corresponding reward and the cost feedback and then move to the next state and due to the non-stationarity of the environment this reward and cost feedbacks may vary Episode by episode so they have a superscript m which denotes the episode of the cmdp and in each episode and the agent aims to maximize the expected total reward while satisfying the constraints on the expected total cost via the formulation of the constraint cmdp and if the data is now stationary it makes sense to make our decision based only on the most recent data that is we need to forget the outdated data such as using the restocking such as using the forgetting based principle like the restart estimation using a slide window was exponentially dedicated ways to estimate the temporary model then a key question is how often shall we forget the outdated data the intuition is that if the non-stationary level is very high in a given period then we may need to discuss the outdated data more often and vice versa and therefore a key notation variation budget is introduced uh in the non-stationary RL to measure the total non-stationary variation in the environment given a period and usually to achieve the Optimal Performance guarantee the forgetting frequency such as how often should should we restart as a model estimation needs to be carefully designed such as that the reset frequency is a sub-linear term of the total variation budget like the uh like it is equal to the order of the B to the power of alpha where I found it to be carefully designed and suppose that the agent execute executes policy Pi i n in the episode and we now Define the optimality Gap the difference between the value of the optimal policy for each episode and the add value of agent's policy for each episode in the long run over the entire episodes Intel capital N episodes and constrain the violation is defined similarly and in this work we design an algorithm called restart optimistic Primo dual PPO and in particular we use a restart policy evaluation and the restart policy Improvement to handle the non-stationarity of the environment and use the optimistic value estimation for the for the exploration and using the Dual update for the safety constraint satisfication and the key challenge is how can we incorporate these three components together to achieve the safe exploration in the presence of non-stationarity with theoretical guarantees and to address this challenge we consider two different scenarios separately the first scenario assumes that each case trend is uniformly feasible and the agent have the agent has access to the local variation budget the local variation budget is defined as the total variation of the environment parameter uh ways in each Epoch or where the epoch itself is defined as a time interval of between the two restarts of the model estimation so uh the first set of assumptions has a weak uh requirement on the safety constraint but it requires uh uh more information on the non-stationary non-station non-stationary environment in the sense that it requires us to have the local information of the variation budget on the other hand we also consider a slightly stronger condition on the safety constraint that is the safety constraints are uniformly strict feasible in the sense that there exists a positive constant gamma such that there is there exists policy pie such that all the constraints are strictly feasible with respect to this a positive constant gamma and due to this jungle condition on the safety constraint we can relax the requirement on the non-stationarity for which we only assume success to the total variation budget over the time periods that we are interested in and we now analyze our algorithm based on these two alternative assumptions and to to stabilize the Dual update under the announced stationarity we will first consider the regularized LaGrange function which has additional regularization on the Dual variable mu and we use the projected gradient Ascent to update the do variable under the assumption of the uniform visibility the two variable can be very large that is this upper bound of the Dual Chi can be potentially unbounded and therefore we need to have the Dual regularization and additional knowledge on the local variation budget to stabilize the do update and on the other hand if we have a slightly stronger condition on the uh feasibility that is the constraints are uniformly strict feasible then the two variable is well bounded that is this upper bounded kind is guaranteed to be a finite number and therefore the due regularization and the local additional knowledge on the local vibration budget is not needed and we started the proposed algorithm under the two alternative conditions that require different amounts of knowledge on the constraints and the results are summarized in this table and both both methods enjoy the dynamic regret and constraint violations which are sublinear in terms of the number of episode M and the total variation barge the B and this is the first uh provably efficient algorithm for the non-stationary cmtps with safe exploration and in our paper we also extend these results from the table MVP to a function approximation setting and finally we verify our method on a simple numerical experiment where we consider a grid world with the time varying ghost days and the obstacle States and the red curves are corresponding to our method which apparently outperforms the Baseline method without considering the non-stationarity when when doing the safe exploration and in addition to the safe requirement in the form of the constraint mdp we also considered a different kind of uh safety requirements which is a risk control and in the risk sensitive reinforcement learning we will consider a mean variance optimization in which we not only want to maximize the expected accumulated reward but also want to control the level of risk which is measured by the variance of the accumulated reward and our goal is to learn a risk sensitive risk sensitive policy which can adapt to the non-stationary environments that is some parameter of the environment may still vary episode that episode however the variance of the accumulated reward is very difficult to estimate in a online environment because for each environment we only encounter a single trajectory so to address this issue we take the lagranting function of this constrained program and find it is related to the Taylor expansion of the entropic risk measurement of the accumulated reward which means that if we take a tail expansion of this uh value function under the entropic risk measure it is equal to the expectation of the accumulated reward plus the beta times the variance of the accumulative reward so if you regard this beta as a penalty term or the new variable in the electronic function it you know this Taylor expression is approximately equal to the lagranging function of this mean various optimization problem however such formulation is still intractable due to the non-linear non-linearity and nonlinear non-linear coupling of this log operation expectation and this exponential operation and therefore it is still difficult to obtain the unbiased samples for for this term foreign and to further address this issue we take the exponential transformation on both sides instead of working directly on the value function under the entropic risk measure we can work on the exponential of the value function which is apparently straightforward to sample and estimate and for this formulation we propose two methods based on the knowledge of the variation budget so if the variation budget is now a prior we can use the forgetting based method which we discussed in the uh in the previous slides such as restart estimation or sliding window was exponential related ways and apply these techniques on the exponential value function and the restart frequency is also carefully designed based on the knowledge of the variation budget to minimize the dynamic regrets however the variation budget is usually unknown in practice and therefore we want to propose an algorithm which can adaptively detect the non-stationarity of the exponential value functions the idea is that if we can detect the event that the observed regret is abnormally larger than the Prime Minister Bound in the stationary setting then we can detect that there must have some solutionarity of the environment happen and we are yeah how can we get the aberration budget what is the immigration budget here yeah so variation budget is what we present here that is met it matches it is a measurement of the total non-stationary variation in the environment given a period m okay so for example in some case you may have a good estimation of the house system where vary you know over the last for example 100 or 200 episodes so in that sense you have uh if you have a good estimation of the vibration budget then you can use this forgetting based method to uh to design an optimal forgetting frequency but uh but uh usually uh this uh good estimation of the variation budget is not always no therefore we want to also propose a method uh which does not require such prior knowledge which is what I'm going to uh talk now okay yeah so it is a scenario where this variation budget is I don't know what we want to do is that we can uh design a non-stationary detection mechanism based on uh the appreserved regret observed required and the theoretically promised about and for example in the risk sensitive uh RL we can upper bound to the dynamic program by the following two terms the first term measures a difference between the expression of the optimal value and the exponential of the our value uh value estimation and the second term measures difference between the exponential of our value estimation and the exponential of the true value of the current policy and luckily uh if the environment is stationary we have very a clear promised band for the R1 and R2 terms for example if this environment is stationary we can know that R1 should always be non-positive and R2 is bounded by a term which is proportional to the square root of the number of total episodes so if we can directly or preserve the R1 and R2 and compare it with its promised promised Bond we can use that as a detection of the non-stationality for example if we find that R1 is uh is greater than zero then it means uh promised bound is violated which means that the underlying assumption for this promised bound is violated which means that this system is not stationary and once we detect that the system is not stationary we can restart our estimation of the system however the challenging part is that this uh this term which I highlighted using a blue box it cannot be directly observed because we never know the true uh optimal policy in advance uh so to address the the challenging that the the first term R1 cannot be directly observed we can fully utilize the optimistic estimator property of our policy evaluation so uh what does so optimistic estimator as it names imply it always provides a optimistic estimation of the value function which means that at any time in a stationary environment there's a optimistic estimated value function should always greater than the up than the optimal value function show by this blue curve and this is what we mean by an optimistic estimator and once another stationary was announced stationarity happens for example if the non-station happens so that the optimal value function jumps up from the from this level to this level and because of the optimal value function the optimal value increase if we learn in this new environment our observed value will also gradually become larger and larger since we are learning the optimal value in this new environment and gradually this observed value will exceed the previous optimistic estimator which indicates that non-stationarity must happen because in a stationary environment the optimal value of our observed value should always uh below this Optimus optimistic estimator and whenever this crossover happens it it helps us detect the non-stationarity of the environment and after the detection of the Annunciation environment we can restart our estimation of the environment of the model yeah so based on this two mechanism or handling the non-stationarity based on why so we have the knowledge of the total variation budget we propose two algorithms the first one is the restart based algorithm with the knowledge of the total variation budget and the second is adaptive detection based method which does not require the knowledge of the total variation budget and the restart based algorithm requires the knowledge of the total variation variation budget so that it is not parameter free and on the other hand adaptive-based method is parameter free and once uh instead for conclusion is that the risk control and the handling of the nurse stationarity can be separately designed if the variation budget is not a player what I mean by that by separation separately designed is that we can handle the non-stationality and the risk control separately that is one way designed this restart mechanism by deciding how frequent shall we restart our estimation this restart frequency does not depend on the risk the sensitive parameter data however on the other hand if we are applying the Adaptive detection based algorithm the non-stationality detection depends on the risk control parameter and therefore their design are always coupled and cannot be separated and finally uh we will move our we move to a sighting uh where we have multiple tasks with a clear task boundaries using the idea from Matalan which meta learning has widely been used as a learning to learn framework to solve unseen tasks with limited experience and in particular in this section in this session we will study what we call the meta safe reinforcement learning framework and in this work we started the problem of the matter safe reinforcement learning through the constraint cmdp within online framework and each within task solves a constrained mdp problem in this upper box and The Meta learner aims to sequentially update the meta initialization for the future new tasks given the knowledge of the landscape policy on the previous tasks and the goal of the safe meta safe reinforcement learning is to design a mechanism such that the task average sub-optimal ticket and the task averaged constraint violation is minimized so in this Matrix we are taking the average over the total number of episodes or total number of tasks and for each uh for each task we measure the difference between the value of the optimum policy and the value of the lender policy and the constraint violation is measured similarly by comparing difference between the Benchmark and the value function corresponding to the cost for the given land policy and the expectation is that this performance measurement should be in should improve as we solve more and more similar tasks because we can learn from the similar prior experience for the future task and to solve each uh within task cmdp we focus on a method called constraint rectified policy optimization crpo it is a pure Primal based online cmdp algorithm which switches between the reward maximization and the safety and satisfication in particular it will perform the objective optimization with the constraints are all satisfied and it will perform the constraint minimization if there is a violated constraint and the main property of this method is that it doesn't require to involve the due variable and therefore when we do the meta initialization our meta learner does not need to learn the meta initialization for the do variable and therefore it becomes the met the outer loop meta learning much easier and under some mild conditions this algorithm also enjoys approvable guarantees on the sub-optimatic Gap and constrain the violation and we can observe from this upper bound that the task average performance guarantee and constraint violations are outbounded by terms based on the difference based on the distance between the optimal policy and the initial policies if the initial policy is very close to the operating policy this outbound is supposed to be very small and the Crux of our idea is to utilize these upper bounds and design a metal algorithm that can sub that can sequentially update to the initial policy by 0 and for each task by performing the online learning on this promised upper bounds so in the idea setting if all the parameters in this upper bounds such as the pi star n and the MU stand which represents the optimal policy and the cross and the corresponding induced State distribution for each task if this parameters are known then this upper Bond can be regarded as a combined function of the initial policy and then we can we can consider this function f n of the initial policy as the individual loss function in our online convex optimization setting and apply the methods such as follows the regularize the leader to sequentially update the initial policy for the future new task and in The Meta safe reinforcement learning framework we expect the task average automatic Gap and the task average constraint violation to improve with the similarity among this online cmdp tasks and we now discuss the notations and of the similarity in the static environment and given the optimal policy for every task T the task similarity can be measured by this term which measures the difference between the optimal policies uh and a single best initial policy in hindsight so comparing these two scenarios we can see that the second scenario has a similar oh has a smaller task similar similarity since all the optimum policies for different tasks are more close to closer to each other and uh this uh notion of task similarity in the static environment is nature for starting the gradient-based meta reinforcement learning as it implies that there exists a best meta initialization in hindsight with respect to which Optimum policies for individual tasks are all close together and with this measurement of task similarity we can present our main results as shown in this group box in the ideal setting we always assume that we have knowledge of all the required parameters we can show that meta learning our meta learning framework is able to guarantee that the task averaged augment Gap and constrain the violation is proportional to the task similarity which means that if the tasks are very similar then we should learn very efficiently and with the guaranteeing on the safety requirement also and this reveal this result reveals the benefits are brought by including more tasks with more similarity and our result is also the first to establish such relationship or even for the unconstrained case now we want to extend the previous result in the idea setting to the more practical setting where we only have access to the exact estimation of zero Cloud knowledge in our framework The Meta lender only requires the inexact optimal policies for each within task cmdp and the corresponding induced State vegetation distributions estimated using the collected offline trajectories to conduct the upper bounds to construct the upper bounds on the sub-optimal Gap and the consumer violations and in addition we also we can also adaptively update the learning rate for each cmdp task that is this learning rate which is denoted by the notation frm can also be adaptively learned such that we can adjust this learning rate for the upcoming new tasks to make the learning more efficient and the idea is similarly we can still regard this upper bound as a convex function of the learning rate and and then apply the online comebacks optimization algorithm to sequentially update the learning rate for the upcoming new tasks foreign in the idea setting We compare our land policy initialization sequence with a single best initial policy in hindsight however image settings we have a change environment in the sense that the the competitor sequence or others uh sequence or the policy initialization we want to compare is varying over the task and in this case uh against nature to study the dynamic progress and Compares with a sequence of potentially temporary initial policies and our method also has a generalization for this more practical setting and finally in the numerical experiments which demonstrates the effectiveness of our proposed metastive RL method and compare with some uh popular baselines the space baselines includes follows the average leader the simple averaging and the pre-trained and random initialization of the uh of the police of the previous policy initializations and the follow the average leader basically use the uh the average of the initial policies from the previous tasks to adaptively uh uh update uh average uh uh initial policy for the upcoming new tasks and the simple averaging user consider also a trained task and use also use the average of all the initial policy policies from the previous encountered task as the initial policy for our testing task and the pre-trained method just use the policy where optimal policy we learned from the previous task as the initial policy for the upcoming new task and in our environment setup we will generate different tasks using different parameters of the cmdp and we will run all the algorithms online where tasks are encountered sequentially and the results we are going to present are just for the testing task with us with the specified policy initialization methods for example in the simple frozen lake example uh where we have we have the different locations of the orientation and the whole which give us a different safe RL tasks and when the task similarity is low we can observe that the methods with RL achieves higher rewards and lower constraint violation much quicker and much more quickly than the base initializations and when the task similarities are very high which means all the tasks are all very similar we can observe that the Baseline pre-trained and the follow average leader are also competitive with the meta safe RL in terms of the reward maximization and the constraint violations and the good performance of these Baseline methods can be explained using the fact that the test task and the training tasks are all very similar the policy initialization will also be very close to the optimal policies of the testing task and we also demonstrate our method using the acrobot which is which has a two which is a two-link robot open AI gym which has a continuous State space the agent is rewarded when it achieves a certain height of of the end link and two constraints are introduced for the two links and we randomly generate 50 different tasks with different Mass links and the center of the gravity and similarly we can observe that for the acrobot environment the metasive IL achieve higher rewards quickly and the error constrain the violation as compared to other Baseline initializations and is the low low task relating settings or the high task related settings and similarly we demonstrate our method uh uh using the have cheetah environment and the humanoid environment and because of some reason I will just uh you know uh leaves this details and in summary this project developed uh develops a set of computational tools with a rigorous analysis for the design of safe RL algorithm in the presence of non-stationarity and in particular we consider the non-stationary constraint mdp so now stationary risk sensitive RL and The Meta safe reinforcement learning framework and based on our current results the future is investigation includes incorporation uh more accurate detection and prediction of transdationality and also gets the agent to adapt the safety uh with adapt adapted safely without forgetting the critical strategies Or the critical safety requirements learned from prior tasks and we also want to generalize these results to a set of formulations Beyond The Matrix of the accumulated reward to include the scenarios like the imitation learning or the pure exploration tasks Etc and finally we want to design a set of Benchmark problems and case studies to evaluate the developed algorithms and I would also like to discuss some details of this uh future directions the first one is the continual continual balancing among the constraints and objective which is a very unique uh challenge in the safeness non-station safe aisle because uh in the non-station safe area we want to continuously uh our goal is not just a singularly optimized reward we want we need to balance between the constraint violation and the objective maximization and this is more challenging when both of them are non-stationary so the limitation of the pro of the proposed Prime module based method for the non-stage generation cmdp is that the optimal dual solution is can vary in a non-stationary environment which makes the uh learning and safe exploration more change and what if we can use a Pure Primal based method like this crpo paper we intro we discussed in the metasiv RL framework and if we can uh so can we propose our Pure Primal based method for the non-station cmdp which can dynamically balance reward maximization and safety certification most mostly without the need to update the time varying view variable and the second idea is that how can we deal with the address of child of exploration exploitation and prediction in the non-stationary environment so we all know the trade-off between exploration and exploitation in the standard area but in the non-stationarity but with another stationarity uh we also need to consider the prediction of the of the future environment so so for example if we consider absolute RL with the reward a varies Over episodes and the standard reactive based so so a popular method of to address this is to construct an estimated reward based on our previous experience and usually we will have uh for example if we are using a linear model to estimate the reward we can have this kind of uh uh reward estimation whereas a beta T beta Tower is some uh parameter and this window WT basically specifies the number of data you will use from the previous experience for the future prediction and in the most of the reactive based method this parameter beta T is just the simple average it just takes a simple average of uh a window near to the current uh uh attempt to make an estimation however uh what if we can you know use our proactive based method in which we actively forecast the future for example we use some forecaster to forecast this a reward to build a model and then make a decision and finally uh but it is important to balance the performance in the past and in the future to avoid the cattle's catastrophic forgetting with the safety constraint so what we discussed are also forward directions in this talk which we want to you know have the future to learning uh to to have good performance on a future task however in a bad word direction we want to avoid that our agents forget you know how to perform safely one of very similar task repairs all right so uh a naive idea is that we can use this episodic memory and the gradient rotation to guarantee that for each new set new task we satisfy the constraints that encountered in the uh in the past and that's all I have today and thank you very much for your attention and I'm happy to take any questions thank you okay thanks to the house great talk hey guys any questions um so I think you're currently consider non-stationary in episode wise settings so basically it means that the non-stationary will happen between the episode um between different episodes so but I think another type of non-stationary may happen within an episode it's kind of like a 10 step wise so do you have any idea about how to detect those non-stationary happens within an episode um and how to address this kind of non-stationarity yeah that's a good question so uh so actually so within episode uh non-stationarity uh is kind of uh easy to address in the episode exciting because in in absolute exciting and in absolutely mdp with finite Horizon you can always use this backward based method that you uh and have this uh uh time dependent uh or time step depending on the policy right like the most standard uh uh Bellman backward approach for the final price mdp right you can do this to address the uh the within app so now stationarity for the absolutely mdp but I think uh what you may really mean is that what if we have a infinite Horizon MVP yeah yeah right and uh where we you know won't have this a chance to re-initialize our trajectory and we need to infinite we need to encounter we need to interact with agents in uh uh in a setting where we you know are not able to initialize I think in this case uh we can still obtain a similar result but we will require some knowledge or some assumptions on this infinite Horizon mdp and some standard assumption is a literature is that it requires some something called the uh the the mixture of this mdp that basically means that during the two guarantees that your uh your mdp allows you to you know return to the some some state or some specified state within some uh time so I think with this requirement on the infinite price and VP you can have the similar results okay thank you so much thank you very question so I think maybe they know stationary safer safe or maybe very useful for Mar cfmr because one important feature MRI is no stationary ridges yeah I think yeah maybe we can work together naturally yeah yeah I think the math agent RL is a nature setting where you know from each agent's perspective they are facing a non-station narrative but this non-stationarity is uh inherent uh is implicitly uh generated by other agents instead of the environment itself so I think uh I I'm always very curious you know uh can we apply this non-stationary RL based method uh to address the market agent IL because for each single RL agent it's it's like it's facing a non-station environment so you know if we can independently you know so can we design a independent and decentralized uh policy learning for each agent in the smart agent system by you know borrowing the idea from the non-stationary IL and also utilizing this special structure of the non-stationarity in the market agent IL setting you know to learn the tool to learn policies in the for the marketing area yeah yeah I think this will be important yeah asking maybe okay just consider in detail maybe in the way you watch it or yeah and to work on the topic yeah yeah so guys any question if you Maybe we don't have any question maybe you can enter the talk yeah thanks for your joining us for joining us to talk thanks so much okay so have a nice day see you thank you have a great day bye 