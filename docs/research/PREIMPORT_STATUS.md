# Pre-import Research Record Status

The files named `PREIMPORT_*.md` are preserved historical proposal records. They document reasoning performed before the successful complete-corpus import and must not be rewritten as if their original generic references were already `SRC-*`-traceable.

Active decisions must be developed in new post-import workspaces using the pinned corpus, beginning with `POSTIMPORT_EVIDENCE_SYNTHESIS.md` and the model/agent candidate matrix. Historical proposals remain useful, but newer source-traceable documents control where the two conflict.
