name: T-528 PySide6 UI screenshots

on:
  pull_request:
    paths:
      - "src/resilient_agents/desktop/**"
      - "scripts/render_ui_screenshots.py"
      - "tests/test_desktop_protocol.py"
      - "configs/protocols/protocol-v2.0-final.json"
      - ".github/workflows/ui-screenshots.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: t528-ui-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      QT_QPA_PLATFORM: offscreen
      PYTHONPATH: src
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: false

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Qt offscreen runtime
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libegl1

      - name: Install uv
        run: python -m pip install --disable-pip-version-check --quiet uv

      - name: Restore locked project environment
        run: uv sync --locked --group gridworld-prototype --no-progress

      - name: Add bounded UI renderer dependency
        run: uv pip install --quiet "PySide6==6.11.2"

      - name: Validate frozen-protocol presentation boundary
        run: uv run --no-sync python -m unittest discover -s tests -p 'test_desktop_protocol.py' -v

      - name: Render deterministic review screenshots
        run: uv run --no-sync python scripts/render_ui_screenshots.py --output ci-ui-screenshots

      - name: Upload review screenshots
        uses: actions/upload-artifact@v4
        with:
          name: t528-ui-screenshots-${{ github.sha }}
          path: ci-ui-screenshots/
          if-no-files-found: error
          retention-days: 14
